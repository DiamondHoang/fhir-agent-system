services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: fhir-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: fhir_agent
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/01-vector.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d fhir_agent"]
      interval: 5s
      timeout: 5s
      retries: 10

  qdrant:
    image: qdrant/qdrant:latest
    container_name: fhir-qdrant
    restart: unless-stopped
    ports:
      - "${QDRANT_PORT:-6333}:6333"
      - "${QDRANT_GRPC_PORT:-6334}:6334"
    environment:
      QDRANT__TELEMETRY_DISABLED: "true"
    volumes:
      - qdrant_data:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/localhost/6333' 2>/dev/null"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s

  neo4j:
    build:
      context: .
      dockerfile: docker/Dockerfile.cyfhir-neo4j
    container_name: fhir-cyfhir-neo4j
    restart: unless-stopped
    environment:
      NEO4J_HOST: 0.0.0.0
      NEO4J_AUTH: none
      NEO4J_dbms_security_procedures_unrestricted: apoc.*,cyfhir.*,gds.*
      NEO4J_dbms_memory_pagecache_size: 512m
      NEO4J_dbms_memory_heap_max__size: 1G
      NEO4J_apoc_export_file_enabled: "true"
      NEO4J_apoc_import_file_enabled: "true"
      NEO4J_apoc_import_file_use__neo4j__config: "true"
      NEO4J_dbms_security_procedures_whitelisted: apoc.*,cyfhir.*,gds.*
    ports:
      - "7474:7474"
      - "7473:7473"
      - "7687:7687"
    volumes:
      - neo4j_data:/data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:7474 || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 20s

  cyfhir-express:
    build:
      context: ./cyfhir-express
      dockerfile: ../docker/Dockerfile.express
    container_name: fhir-cyfhir-express
    restart: unless-stopped
    ports:
      - "${CYFHIR_EXPRESS_PORT:-3001}:3001"
    environment:
      PORT: 3001
      NEO4J_URI: "bolt://neo4j:7687"
      NEO4J_USER: "neo4j"
      NEO4J_PASSWORD: "password"
    volumes:
      - ./cyfhir-express:/app
      - express_node_modules:/app/node_modules
    depends_on:
      neo4j:
        condition: service_healthy

  cyfhir-seed:
    image: curlimages/curl:latest
    container_name: fhir-cyfhir-seed
    profiles: ["seed"]
    restart: "no"
    entrypoint: ["/bin/sh", "/scripts/seed-neo4j.sh"]
    environment:
      CYFHIR_EXPRESS_URL: "http://cyfhir-express:3001"
      BUNDLES_DIR: "/data/synthea-bundles"
      FHIR_SERVER_URL: "${FHIR_SERVER_URL}"
    volumes:
      - ./docker/scripts/seed-neo4j.sh:/scripts/seed-neo4j.sh:ro
      - ./data/synthea-bundles:/data/synthea-bundles:ro
    depends_on:
      - cyfhir-express
      - neo4j

  backend:
    build:
      context: ./backend
      dockerfile: ../docker/Dockerfile.backend
    container_name: fhir-backend
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    volumes:
      - ./backend:/app
    environment:
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USERNAME: ${NEO4J_USERNAME:-neo4j}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:-password}
      CYFHIR_EXPRESS_URL: http://cyfhir-express:3001
      FHIR_SERVER_URL: ${FHIR_SERVER_URL}
      LLM_PROVIDER: ${LLM_PROVIDER:-ollama}
      COMPANY_LLM_BASE_URL: ${COMPANY_LLM_BASE_URL}
      COMPANY_LLM_API_KEY: ${COMPANY_LLM_API_KEY}
      COMPANY_LLM_MODEL: ${COMPANY_LLM_MODEL}
      COMPANY_EMBEDDING_BASE_URL: ${COMPANY_EMBEDDING_BASE_URL}
      COMPANY_EMBEDDING_API_KEY: ${COMPANY_EMBEDDING_API_KEY}
      COMPANY_EMBEDDING_MODEL: ${COMPANY_EMBEDDING_MODEL}
      COMPANY_EMBEDDING_DIMS: ${COMPANY_EMBEDDING_DIMS:-1024}
      OLLAMA_LLM_BASE_URL: ${OLLAMA_LLM_BASE_URL}
      OLLAMA_LLM_API_KEY: ${OLLAMA_LLM_API_KEY}
      OLLAMA_LLM_MODEL: ${OLLAMA_LLM_MODEL}
      OLLAMA_EMBEDDING_BASE_URL: ${OLLAMA_EMBEDDING_BASE_URL}
      OLLAMA_EMBEDDING_API_KEY: ${OLLAMA_EMBEDDING_API_KEY}
      OLLAMA_EMBEDDING_MODEL: ${OLLAMA_EMBEDDING_MODEL}
      OLLAMA_EMBEDDING_DIMS: ${OLLAMA_EMBEDDING_DIMS:-1024}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-fhir_agent}
      MEM0_AGENT_ID: ${MEM0_AGENT_ID:-fhir-clinical-agent}
      MEM0_VECTOR_STORE_PROVIDER: pgvector
      MEM0_COLLECTION_NAME: ${MEM0_COLLECTION_NAME:-fhir_agent_memories}
      CORS_ORIGINS: http://localhost:${FRONTEND_PORT:-3000},http://127.0.0.1:${FRONTEND_PORT:-3000}
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/fhir_agent
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-dev_secret_key_change_in_production}
      JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
      JWT_ACCESS_TOKEN_EXPIRE_MINUTES: ${JWT_ACCESS_TOKEN_EXPIRE_MINUTES:-60}
      SKIN_VISION_BASE_URL: ${SKIN_VISION_BASE_URL:-}
      SKIN_VISION_MODEL: ${SKIN_VISION_MODEL:-}
      SKIN_REASONING_BASE_URL: ${SKIN_REASONING_BASE_URL:-}
      SKIN_REASONING_MODEL: ${SKIN_REASONING_MODEL:-}
      SKIN_EMBEDDING_BASE_URL: ${SKIN_EMBEDDING_BASE_URL:-}
      SKIN_EMBEDDING_API_KEY: ${SKIN_EMBEDDING_API_KEY:-}
      SKIN_EMBEDDING_MODEL: ${SKIN_EMBEDDING_MODEL:-}
      SKIN_QDRANT_URL: http://qdrant:6333
      SKIN_QDRANT_COLLECTION: ${SKIN_QDRANT_COLLECTION:-disease_symptoms}
      COMPANY_VISION_URL: ${COMPANY_VISION_URL:-http://172.16.12.230:8000}
      COMPANY_VISION_MODEL: ${COMPANY_VISION_MODEL:-gemma-4-26B-A4B-it}
      COMPANY_VISION_PREFER: ${COMPANY_VISION_PREFER:-gemma-4}
      COMPANY_REASONING_URL: ${COMPANY_REASONING_URL:-http://172.16.12.230:8000}
      COMPANY_REASONING_MODEL: ${COMPANY_REASONING_MODEL:-gemma-4-26B-A4B-it}
      COMPANY_REASONING_PREFER: ${COMPANY_REASONING_PREFER:-gemma-4}
      COMPANY_KB_EMBEDDING_URL: ${COMPANY_KB_EMBEDDING_URL:-http://172.16.12.230:8004}
      COMPANY_KB_EMBEDDING_MODEL: ${COMPANY_KB_EMBEDDING_MODEL:-BAAI/bge-m3}
      OLLAMA_VISION_URL: ${OLLAMA_VISION_URL:-http://host.docker.internal:11434}
      OLLAMA_VISION_MODEL: ${OLLAMA_VISION_MODEL:-gemma4:31b-cloud}
      OLLAMA_VISION_PREFER: ${OLLAMA_VISION_PREFER:-gemma4}
      OLLAMA_REASONING_URL: ${OLLAMA_REASONING_URL:-http://host.docker.internal:11434}
      OLLAMA_REASONING_MODEL: ${OLLAMA_REASONING_MODEL:-gemma4:31b-cloud}
      OLLAMA_REASONING_PREFER: ${OLLAMA_REASONING_PREFER:-gemma4}
      OLLAMA_KB_EMBEDDING_URL: ${OLLAMA_KB_EMBEDDING_URL:-http://host.docker.internal:11434}
      OLLAMA_KB_EMBEDDING_MODEL: ${OLLAMA_KB_EMBEDDING_MODEL:-bge-m3:latest}
      VISION_MODEL_TIMEOUT: ${VISION_MODEL_TIMEOUT:-600}
      REASONING_MODEL_TIMEOUT: ${REASONING_MODEL_TIMEOUT:-1200}
      EMBEDDING_TIMEOUT: ${EMBEDDING_TIMEOUT:-120}
      QDRANT_MODE: server
      QDRANT_URL: http://qdrant:6333
      QDRANT_COLLECTION: ${QDRANT_COLLECTION:-disease_symptoms}
    depends_on:
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_healthy
      neo4j:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: ../docker/Dockerfile.frontend
    container_name: fhir-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
      - frontend_next:/app/.next
    depends_on:
      - backend

volumes:
  postgres_data:
  qdrant_data:
  neo4j_data:
  frontend_node_modules:
  frontend_next:
  express_node_modules:
